# coeiroink2-txt2mp3

## OpenAPI

this cli tool uses openapi-generator-cli to generate a rust server from the openapi.json file.

```
$ openapi-generator-cli generate -g rust-server -i openapi.json -o coeiroink2
```
